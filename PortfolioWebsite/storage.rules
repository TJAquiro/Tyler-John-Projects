rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only the authenticated server uploads. Published files use download tokens.
    match /{allPaths=**} { allow read, write: if false; }
  }
}
