# September 8 scope (historical)

The September 8 user request superseded the original single-owner/password-only brief:

- No automatically seeded mock projects or profile information for new accounts.
- Fix/verify the reported server/client hydration mismatch.
- Account creation and sign-in, with immediate onboarding after signup. Each account has its own portfolio (default chosen while awaiting an optional preference reply).
- A large standardized software catalog plus custom tool names.
- Education descriptions.
- Exact photo cropping.
- Optional descriptions beneath project photos.
- Local dev tools to reset accounts/projects.
- A separate optional complete showcase profile.
- Exactly ONE final critic/review pass; no additional iteration after that report.

Preserve existing owner-created content. Test resets only against disposable QA data. Do not run the real reset or replace the owner's portfolio with the showcase.
# September 9: homepage content and consistent editing

The current request adds a shared full biography on Home/About, a configurable homepage tagline and cropped banner, one explicitly featured project with a prominent description, and project years. All new project/education/experience dates use one MM/DD/YYYY field and calendar (today by default), with readable month/day/year output. Existing incomplete dates retain their original precision until corrected by the owner. Profile and project software selection use the same searchable catalog and custom-entry controls.

Review usability, editability, and consistency across these related controls. Exactly one final independent critic pass follows implementation checks. Findings before revision are in `docs/REVIEW-2026-09-09.md`; final evidence and scores belong in `CRITIC-REPORT.md`. Previous requests below remain historical context.
